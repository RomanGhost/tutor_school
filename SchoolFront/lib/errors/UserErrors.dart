class ShortName implements Exception{
  String cause;
  ShortName(this.cause);
}

class EmailError implements Exception{
  String cause;
  EmailError(this.cause);
}

class PasswordError implements Exception{
  String cause;
  PasswordError(this.cause);
}

